@model CocusFlightPlanner.Application.ViewModel.RouteViewModel

@{
    ViewData["Title"] = "Routes";
}

@section Toolbar {
    <div>
        <span>Routes</span>
    </div>
    <div>
        <a class="btn btn-primary btn-sm" asp-area="" asp-controller="Route" asp-action="Upsert">Create New</a>
    </div>
}

<div class="mt-5">
    @{ await Html.RenderPartialAsync("_listRoutes", Model); }
</div>

@section Scripts {
    <script>
        debugger;
        const deleteModal = document.getElementById('confirmDeleteModal');
        deleteModal.addEventListener('show.bs.modal', function (event) {
            const button = event.relatedTarget;
            const id = button.getAttribute('data-id');
            const form = deleteModal.querySelector('#deleteForm');
            const input = deleteModal.querySelector('#deleteId');

            input.value = id;
            form.action = `/Route/Delete/${id}`;
        });
    </script>
}


